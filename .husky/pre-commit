#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run Ruff linting and formatting on Python files
echo "Running Ruff linter and formatter..."
ruff check --fix .
ruff format .

# Add the changes made by Ruff
git add -u 